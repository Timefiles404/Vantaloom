# Agent Runtime

Agent Runtime 后续需要支持可恢复、可审计、可验证的工程执行。

推荐的运行链路：

```text
用户意图
  -> 计划
  -> 任务图
  -> 工具调用
  -> Git 检查点
  -> CI 验证
  -> 结果解释
  -> 修复循环
```

## 基础原则

- 每次运行都应有结构化事件日志
- 重要步骤应能从最近检查点恢复
- 工具调用应有明确输入、输出和权限边界
- CI 失败应进入解释和修复流程，而不是直接重新生成
- 模型输出不能直接替代验证结果
